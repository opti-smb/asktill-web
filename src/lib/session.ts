/** App JWT lifetime — must match Authentication / Registration services (2400s). */
export const SESSION_TTL_SECONDS = 40 * 60;
export const SESSION_TTL_MS = SESSION_TTL_SECONDS * 1000;
export const SESSION_TTL_LABEL = '40 minutes';
